// components/layout/MobileNav.tsx
// Mobile navigation is handled by the slide-out drawer in Sidebar.tsx,
// triggered by the hamburger button in Header.tsx (see MobileMenuContext).
// This component is intentionally a no-op placeholder kept for the layout
// import; it renders nothing so it no longer leaks stray text onto pages.
export function MobileNav() {
  return null
}